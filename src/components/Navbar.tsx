import React from 'react';

export default function Navbar() {
  return (
    <div>
      <button>Video</button>
      <button>Image</button>
      <button>Memo</button>
      <button>Task</button>
    </div>
  );
}
